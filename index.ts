export * as pax2pay from "./pax2pay"

// below is only for backwards compatibility, no new exports to be added
import { Card } from "./Card"
import { Client } from "./Client"
import * as Proxy from "./Proxy"

export { Card, Client, Proxy }
